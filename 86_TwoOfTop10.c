// Prime Number
#include<stdio.h>
int main()
{
    int arrsize;
    printf("\n Enter the size of array : ");
    scanf("%d",&arrsize);
    int arr[arrsize];
    for(int i=1;i<=arrsize;i++)
    {
        if(i%1==0 || i%i )
    }
}