#include<stdio.h>
int main()
{
    int num;
    printf("Enter a num : ");
    scanf("%d",&num);

    printf("%d",num/2==0);// if output become zero the num is not divisible by 2 or if output became 1 the number is divisible by 2

    //we can also check wethear the given number is even or odd like follows;
    // 0 for odd or 1 for even 
}